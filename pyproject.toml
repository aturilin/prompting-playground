[project]
name = "prompting-playground"
version = "1.0.0"
description = "Test prompts across multiple LLM models"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "python-dotenv",
    "pydantic",
    "pyyaml",
    "supabase",
]

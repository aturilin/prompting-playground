# Specific Constraints - precise format control
name: CONSTRAINTS
description: Explicit limits on format, length, structure

template: |
  {{task}}

  Constraints:
  - Length: {{length}}
  - Structure: {{structure}}
  - Must include: {{must_include}}
  - Avoid: {{avoid}}
